@* @model PIOGHOASIS.Models.ViewModels.ReservaResumenVM

<div class="rs-summary__block">
    <div class="d-flex align-items-center gap-2 mb-2">
        <div><strong>@Model.CheckIn.ToString("MMM dd, yyyy")</strong></div>
        <div>→</div>
        <div><strong>@Model.CheckOut.ToString("MMM dd, yyyy")</strong></div>
        <div class="ms-auto small"><i class="bi bi-moon"></i> @Model.Noches Noche(s)</div>
    </div>

    <hr class="my-2" />

    <div class="mb-2 fw-bold">@Model.HabitacionTitulo</div>
    <div class="text-muted small mb-2">
        @(Model.TarifaID.HasValue ? "Tarifa seleccionada" : "Tarifa estándar")
    </div>
    <div class="mb-3"><i class="bi bi-people"></i> @Model.Personas persona(s)</div>

    <div class="d-grid gap-1 mb-2">
        <div class="d-flex justify-content-between">
            <span>Subtotal</span><span>Q. @Model.Subtotal.ToString("N2")</span>
        </div>
        <div class="d-flex justify-content-between">
            <span>Impuestos</span><span>Q. @Model.Impuestos.ToString("N2")</span>
        </div>
    </div>

    <div class="d-flex justify-content-between fs-5 fw-bold mb-3">
        <span>Total</span><span>Q. @Model.Total.ToString("N2")</span>
    </div>

    <div class="rs-summary__footer">
        <a class="rs-btn-muted" href="/Reservas/Cliente">Reservar Ahora</a>
    </div>
</div> *@