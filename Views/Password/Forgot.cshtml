@model ForgotVm
@{
    Layout = null;
    ViewData["Title"] = "Restablecer contraseña";
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/login.css" />
</head>
<body>
    <div class="login-shell">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-lg-10">
                    <div class="card-glass">
                        <div class="row g-0 align-items-stretch">

                            <!-- Columna izquierda: logo -->
                            <div class="col-12 col-md-5 position-relative brand-box">
                                <img src="/img/login/logo-oasis.png" alt="Hotel Oasis Logo">
                                <span class="v-divider" aria-hidden="true"></span>
                            </div>

                            <!-- Columna derecha: formulario -->
                            <div class="col-12 col-md-7 form-side">
                                <h5 class="title">Restablecer contraseña</h5>
                                <h5 class="subtitle">Hotel Panajachel Oasis</h5>

                                <p class="text-muted small mb-3 mt-3">
                                    Ingresa el correo electrónico asociado a tu cuenta.
                                    Te enviaremos un enlace para restablecer tu contraseña,
                                    válido durante los próximos <strong>5 minutos</strong>.
                                </p>

                                @if (TempData["ForgotOk"] is string ok)
                                {
                                    <div class="alert alert-success alert-dismissible fade show py-2" role="alert">
                                        @ok
                                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                    </div>
                                }

                                @if (TempData["ForgotError"] is string err)
                                {
                                    <div class="alert alert-warning py-2">@err</div>
                                }

                                <form asp-action="Forgot" method="post" novalidate autocomplete="off">
                                    @Html.AntiForgeryToken()

                                    <div class="mb-3">
                                        <label class="form-label fw-semibold">Correo electrónico</label>
                                        <input asp-for="Email" class="form-control oasis-input" type="email"
                                               placeholder="ejemplo@correo.com" required />
                                        <span asp-validation-for="Email" class="text-danger small"></span>
                                    </div>

                                    <div class="d-flex flex-column flex-sm-row align-items-center justify-content-center gap-3 mt-4">
                                        <button type="submit" class="btn btn-oasis">
                                            Enviar enlace
                                        </button>
                                        <a class="btn btn-outline-secondary" href="/Login/Index">
                                            Volver al inicio de sesión
                                        </a>
                                    </div>
                                </form>
                            </div>

                        </div>
                    </div>
                    <div class="py-2"></div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <partial name="_ValidationScriptsPartial" />
</body>
</html>



@* @model ForgotVm
@{
    Layout = null; ViewData["Title"] = "Restablecer contraseña";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
<div class="container py-4" style="min-height:100vh;display:flex;align-items:center;justify-content:center;background:
    linear-gradient(rgba(255,255,255,.35),rgba(255,255,255,.35)), url('/img/login/fondo.jpg') center/cover no-repeat;">
    <div class="card shadow" style="max-width:640px;width:100%;background:rgba(255,255,255,.92)">
        <div class="card-body p-4">
            <h5 class="text-danger fw-bold">Instrucciones</h5>
            <ul class="small text-muted mb-3">
                <li>Escribe el correo electrónico asociado a tu usuario.</li>
                <li>Te enviaremos un correo con un enlace válido por 5 minutos.</li>
            </ul>

            @if (TempData["ForgotOk"] is string ok)
            {
                <div class="alert alert-success py-2">@ok</div>
            }

            <form asp-action="Forgot" method="post" novalidate>
                @Html.AntiForgeryToken()
                <div class="mb-3">
                    <label class="form-label fw-semibold">Correo electrónico</label>
                    <input asp-for="Email" class="form-control" type="email" placeholder="ejemplo@correo.com" />
                    <span asp-validation-for="Email" class="text-danger small"></span>
                </div>
                <button class="btn btn-warning fw-bold">Enviar</button>
                <a class="btn btn-outline-secondary ms-2" href="/Login/Index">Volver</a>
            </form>

            <partial name="_ValidationScriptsPartial" />
        </div>
    </div>
</div>
 *@